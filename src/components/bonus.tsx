import * as React from 'react';
import '../styles/bonus.scss';
import { Utilities } from '../utilities/utilities';

class State {
};

interface Props {
};

export class Bonus extends React.Component<Props, State> {
    render() : JSX.Element {
        return <div>

               </div>;
    };
};